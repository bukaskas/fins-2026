import { NavScrollWrapper } from "./NavScrollWrapper";
import Menu from "./menu";

function Header() {
  return (
    <NavScrollWrapper>
      <Menu />
    </NavScrollWrapper>
  );
}

export default Header;
